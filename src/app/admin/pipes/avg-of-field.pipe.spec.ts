import { AvgOfFieldPipe } from './avg-of-field.pipe';

describe('AvgOfFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new AvgOfFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
