import { HScrollDirective } from './h-scroll.directive';

describe('HScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HScrollDirective();
    expect(directive).toBeTruthy();
  });
});
